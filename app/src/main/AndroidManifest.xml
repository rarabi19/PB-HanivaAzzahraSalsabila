<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApps"
        tools:targetApi="31">

        <!-- MainActivity2 sebagai activity utama -->
        <activity
            android:name=".MainActivity2"
            android:exported="true"
            android:theme="@style/Theme.MyApps"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".HomeActivity"
            android:exported="true" />

        <activity
            android:name=".AlquranActivity"
            android:exported="true" />

        <activity
            android:name=".AsmaActivity"
            android:exported="true" />

        <activity
            android:name=".DoaActivity"
            android:exported="true" />

        <activity
            android:name=".JadwalActivity"
            android:exported="true" />

        <activity
            android:name=".KalenderActivity"
            android:exported="true" />

        <activity
            android:name=".KiblatActivity"
            android:exported="true" />

        <activity
            android:name=".UserActivity"
            android:exported="true" />

        <!-- Tambahkan activity lain di sini jika ada -->
    </application>

</manifest>
